
export type FileUploadRequest = {
	// Defines the file types the file input should accept. See the JavaScript file description of "accept"
	// for more informations. E.g.: .doc,.docx,.xml,application/msword
	accept: string
}